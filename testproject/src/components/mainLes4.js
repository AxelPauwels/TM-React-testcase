import React from 'react';
import WeatherListContainer from "../containers/les4_container_weather_list";

const MainLes4 = (props) => {
    return (
        <WeatherListContainer/>
    );
};

export default MainLes4;
