import React from 'react';

const Home = (props) => {
    return (
        <div>
            <p><span style={{display: "inline-block", width: 90}}>LesX</span> = de powerpoint voorbeelden</p>
            <p><span style={{display: "inline-block", width: 90}}>LesX-oef</span> = de oefeningen</p>
        </div>
    );
};

export default Home;
